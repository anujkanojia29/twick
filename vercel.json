{
  "version": 2,
  "buildCommand": "apt-get update && apt-get install -y python3-distutils build-essential libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev && pnpm install && pnpm run build",
  "outputDirectory": "packages/examples/dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
