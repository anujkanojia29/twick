{
  "version": 2,
  "buildCommand": "apk add --no-cache build-base cairo-dev pango-dev jpeg-dev giflib-dev python3 py3-pip && pip3 install --break-system-packages distutils && pnpm install && pnpm run build",
  "outputDirectory": "packages/examples/dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
